enum ChaptersVideosUrlLocation { cssClass, none, empty }

enum ChaptersVideosUrlParseModes { jsonList, empty }

enum VideoUrlParsers { yourUpload, streamTape, streamWish, none }

enum SupportedThemes { catppuccin, fallback, dynamic }
