import 'package:flutter/material.dart';

class EndScrolledViewPadding extends StatelessWidget {
  const EndScrolledViewPadding({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 60);
  }
}
