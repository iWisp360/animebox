import 'package:flutter/material.dart';

class TrackersTab extends StatelessWidget {
  const TrackersTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("trackers"));
  }
}
