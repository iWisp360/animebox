import 'package:flutter/material.dart';

class BrowsePageView extends StatelessWidget {
  const BrowsePageView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Browse"));
  }
}
